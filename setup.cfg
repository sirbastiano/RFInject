[metadata]
name = your_package_name # Replace with your package name

[options]
packages = find:
package_dir =
    = src
install_requires =
    # Add dependencies here if not using pyproject.toml for them, or for older pip versions

[options.packages.find]
where = src
